// Generated by ts-to-zod
import { z } from "zod";

export const testCallV1Schema = z.object({
  email: z.string().email(),
  confirmed: z.boolean().default(false),
});

export const testCallV2Schema = z.object({
  email: z.string().email(),
  confirmed: z.boolean().default(false),
  name: z.string().optional(),
});

export const clientApiV1Schema = testCallV1Schema;

export const clientApiV2Schema = testCallV2Schema;

export const clientApiSchema = z.union([
  z.object({
    version: z.literal(0),
    call: z.null(),
  }),
  z.object({
    version: z.literal(1),
    call: clientApiV1Schema,
  }),
  z.object({
    version: z.literal(2),
    call: clientApiV2Schema,
  }),
]);
